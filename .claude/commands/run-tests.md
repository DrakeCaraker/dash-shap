# Run Tests

Run the full pytest suite and summarize results:

1. Run `pytest -v --tb=short` from the repo root
2. Summarize: total passed, failed, errors, warnings
3. If any failures, show the failing test names and short tracebacks
4. Do NOT fix failures automatically — report them and ask the user what to do
