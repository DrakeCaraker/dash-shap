# Release Readiness Audit — 2026-03-24

Checked packaging, API docs, CLAUDE.md commands, test coverage, and version export.

## Packaging

`pyproject.toml` present with all required fields (`name="dash-shap"`, `version="0.1.0"`, 10 runtime dependencies). Build system uses setuptools. No issues found.

## Version Export

`python3 -c "import dash_shap; print(dash_shap.__version__)"` succeeds and prints `0.1.0`.

## CLAUDE.md Accuracy

All commands in the "Running" section verified:
- `run_experiments.py` exists at repo root
- `run_experiments_parallel.py` exists at repo root
- `scripts/check_notebook_ids.py` exists
- Makefile targets `test`, `test-fast`, `lint`, `fmt`, `typecheck`, `coverage`, `rebase` all present and correctly defined

No issues found.

## Test Coverage

Total coverage: **72.70%** (294 passed, 1 skipped, 27 deselected via `-m "not slow"`). Passes the 70% floor.

Modules below 50% coverage:

| Module | Coverage |
|--------|----------|
| `dash_shap/baselines/random_selection.py` | 14% |
| `dash_shap/core/diagnostics.py` | 18% |
| `dash_shap/baselines/stochastic_retrain.py` | 24% |
| `dash_shap/extensions/__init__.py` | 33% |

## Public API Docstrings

63 public API items (classes, methods, functions) lack docstrings. Key gaps by module:

- **baselines/** (8 classes, 16 methods): All baseline classes and their `__init__`/`fit`/`fit_from_population` methods missing docstrings (EnsembleSHAPBaseline, LargeSingleModelBaseline, NaiveAveragingBaseline, NNBaselines, PermutationImportanceBaseline, RandomForestBaseline, RandomSelectionBaseline, SingleBestBaseline, StochasticRetrainBaseline)
- **core/pipeline.py**: `DASHPipeline` class and 4 public methods (`get_consensus_ensemble_predictions`, `get_fsi`, `get_importance_ranking`, `plot_importance_stability`)
- **core/diagnostics.py**: `FeatureStabilityIndex` and `ImportanceStabilityPlot` classes plus 4 methods
- **core/result.py**: 4 properties (`K`, `P`, `memory_bytes`, `n_ref`)
- **core/rashomon_search.py**: `RashomonSurrogate.__init__`
- **experiments/schemas.py**: 7 dataclass definitions
- **extensions/**: 12 `plot()` and `summary()` methods across 6 extension result classes

## Findings

- [MEDIUM] `dash_shap/baselines/random_selection.py` — test coverage 14%, well below 50% floor
- [MEDIUM] `dash_shap/core/diagnostics.py` — test coverage 18%, well below 50% floor
- [MEDIUM] `dash_shap/baselines/stochastic_retrain.py` — test coverage 24%, well below 50% floor
- [MEDIUM] `dash_shap/extensions/__init__.py` — test coverage 33%, below 50% floor
- [MEDIUM] `dash_shap/baselines/` — 8 baseline classes and 16 public methods missing docstrings
- [MEDIUM] `dash_shap/core/pipeline.py` — `DASHPipeline` class and 4 public methods missing docstrings
- [MEDIUM] `dash_shap/core/diagnostics.py` — 2 classes and 4 methods missing docstrings
- [MEDIUM] `dash_shap/core/result.py` — 4 public properties missing docstrings
- [MEDIUM] `dash_shap/experiments/schemas.py` — 7 dataclass definitions missing docstrings
- [MEDIUM] `dash_shap/extensions/` — 12 public methods across 6 result classes missing docstrings

**10 issues found (0 CRITICAL, 0 HIGH, 10 MEDIUM, 0 LOW)**
